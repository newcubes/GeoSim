# Placeholder for Vercel

If your Vercel project has **Root Directory** set to `packages/collab`, this folder exists so the project still builds. All commands delegate to the repo root; the actual app is built from there.

To build from the real root instead, set Root Directory to **empty** (or `.`) in Vercel → Settings → General.
